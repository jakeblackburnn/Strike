# Scratch

This file exists to be hidden. `../strike.yaml` lists `guide/scratch.sx` under
`hidden:`, so it has no nav entry and `/example/guide/scratch` returns 404 —
even though the file sits right here next to `main.sx`.

Delete the `hidden:` entry and this page appears in the nav immediately.
