# Devlog 2026-09

## 2026-09-18 15:40 · Two-agent session: typography, themes, flow(2), PDF backend
- **changed:** `sheet.zig`/`shell.zig`: `.sxh` gets real typography (font/measure/size/leading), threaded to page CSS with reader-choice-over-header-over-site precedence
- **changed:** `shell.zig`: season/theme table collapsed to one source of truth, added kanagawa/vanta-black, reader-selectable fonts (mono/humanist), wider sidebar collapse hitbox
- **changed:** `theme_file.zig` (new): build-time-validated external theme file format + `theme_file:` yaml key — closes the goal-5 "themes as files" gap
- **changed:** `strikedown`/`render_html`/`render_pdf.zig` (new): `flow(2)` paginated-column command (design note 022, shipped) + native Zig PDF backend walking the parsed tree; `docs/paper/` sample ties header+theme+flow+PDF together
- **why:** session goals were reader-UI overhaul (themes/fonts/menus) and deep project-config threading (typography headers, page layout, PDF) — split as Claude=shell.zig/reader UI, Codex=config/language/PDF to avoid file collisions
- **verified:** `zig build` clean; `zig build test --summary all` → 31/31 steps, 1759/1759 tests passed (repeated 3x, stable)
- **by:** pair (Claude + Codex, coordinated via shared plan file and canvas)
- **next:** uncommitted — 16 files + 4 new (see plan file); commit as a version bump before anything else touches this tree

## 2026-09-18 17:20 · Close-out: commit v0.2.0, tests, decisions, stale markers
- **changed:** `docs/paper/strike.yaml`: `theme: kanagawa` → `custom`, so the sample selects its own `theme_file` palette instead of shadowing it; regenerated `paper.pdf`
- **changed:** `build.zig`: test step walks `src/` recursively (31→39 steps) so `src/strikedown/*.zig` are finally test roots, matching what the step's own comment always claimed
- **changed:** added tests to `theme_file.zig`, `themes.zig`, `render_pdf.zig` (rejection paths, table invariants, page-size/margin/typography handling) — the three files that landed last session with the thinnest coverage
- **changed:** `build.zig.zon` → 0.2.0; dropped a stale `// ---- v0.1.0 fixes ----` marker in `main.zig`; `dev/DECISIONS.md` created (flow(n) Candidate A, native Zig PDF over Chromium)
- **why:** last session shipped four features but never committed and never wrote the decision record `HANDOFF.md` pointed at twice
- **verified:** `zig build` clean; `zig build test --summary all` → 39/39 steps, 1819/1819 tests passed; built `docs/` to static HTML and confirmed the paper page defaults to `data-season="custom"` with Paper Ink inlined; `pdfinfo` on the regenerated PDF (2 pages, letter)
- **by:** claude
- **next:** committed as `056d7bb`; deferred pagination policy (widows/orphans, oversize blocks, in-flow `span()`) and PDF gaps (images, math, non-ASCII) still open, both want a design note first
